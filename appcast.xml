<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>StatsBar</title>
    <link>https://raw.githubusercontent.com/Matrix-I/stats-bar/main/appcast.xml</link>
    <description>Most recent StatsBar updates.</description>
    <language>en</language>
    <!-- APPCAST_ITEMS: update_appcast.sh prepends each release's <item> immediately below this line. -->
    <item>
      <title>Version 2.12.3</title>
      <pubDate>Sun, 09 Aug 2026 17:23:31 +0700</pubDate>
      <sparkle:version>2.12.3</sparkle:version>
      <sparkle:shortVersionString>2.12.3</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
      <sparkle:fullReleaseNotesLink>https://github.com/Matrix-I/stats-bar/releases/tag/v2.12.3</sparkle:fullReleaseNotesLink>
      <description><![CDATA[# StatsBar v2.12.3 — the RAM figures against the RAM that exists, and two crashes that were waiting for the wrong Mac

Ten fixes and no new panels, so another patch number; `2.13.0` stays reserved for the release that adds something. **The memory percentage will go up by about three points when you install this, and that is the fix rather than a symptom** — read the first item before wondering what started using more RAM.

Two of the ten are crashes rather than wrong numbers. Neither has ever fired on the Mac they were found on, and both were reachable from ordinary hardware readings, which is the combination worth shipping a release for.

- **Fixed: every memory figure was measured against RAM that does not exist, and Free was whatever was left over.** macOS reserves a firmware carveout the kernel's page counters never account for — 0.60 GiB of the 16 on an M1 Pro, published as the gap between `hw.memsize` and `hw.memsize_usable` — and every fraction in the panel divided by the larger number. So the usage ring read 81% where the truth was 84%, the colour ramp changed a band late, and the menu-bar percentage could not reach beyond 96% however hard the machine was working. Free was worse, because it was not measured at all: it was the nameplate minus the other buckets, so the entire carveout landed inside it. The panel reported 0.66 GB free on a machine that was 3.11 GB into swap with 0.05 GB genuinely free, and the row had a floor of 0.60 GB it could never print below — a figure least trustworthy exactly when someone looks at it, with the relative error measured between 2.1× and 12.75× as memory ran out. Free is now read from the kernel like every other bucket, the fractions divide by the RAM actually under management, and what the buckets do not cover is named **Reserved** in the details list instead of being quietly called free. This is the third fix in this file's history and the second of exactly this shape: a remainder absorbs anything the buckets fail to name, and last time it was 3.2 GB of file cache. A remainder cannot be wrong about only one thing.

- **Fixed: TOP PROCESSES listed the wrong processes.** The list was ranked by `ps -m`, which sorts by resident size, while the column beside each name showed the phys-footprint figure Activity Monitor calls "Memory" — two different measurements, so the six rows shown were the top six of a list nobody was reading. Three of the six differed from the correct answer when it was checked. That is a membership error rather than a rounding one, and every row in that table carries a Quit menu.

- **Fixed: a sensor reading that is not a number took the app down.** `Int(someDouble)` in Swift is a trapping conversion, not a saturating one: it halts the process on NaN, on either infinity, and on anything past Int64's range, and the shipped build keeps that check. Fourteen places wrote the conversion inline over values that come from hardware — fan speeds, die temperatures, refresh rates, battery percentages. The SMC produces a `flt ` reading by reinterpreting four bytes as a float, and every bit pattern is a legal float, NaNs included, so a fan index a machine does not have or a key that means something else on another chip is enough. Measured here as an immediate SIGTRAP for all four classes. Readings that are not numbers are now refused where they are read, and the conversions saturate.

- **Fixed: a private CPU-frequency symbol could take the app down by returning a negative count.** `IOReportStateGetCount` is looked up at runtime out of a library with no public header and returns a signed integer, which is how a C interface says it can fail. That value was used directly as a loop bound, and a negative bound is not an empty loop — it is the same halt as above. The rest of that file already treats the whole interface as something that may disappear at any macOS release; the one place it did not was the value the interface hands back.

- **Fixed: the network rate in the menu bar could print "1000 KB/s".** It chose a unit by comparing against 1000 and then rounded inside the unit it had chosen, so the top of each band rounded straight past the band — "1000 B/s", "1000 KB/s" and "1000.0 MB/s" are all readings its own scale says should already have become the next unit up. Compared against the previous version across 24.6 million sampled rates, they disagree at 5,370 of them, all in the three narrow windows below a boundary. Every other formatter in the app was fixed for this months ago; this one sat in a file the check could not see, which is why it has moved rather than been patched where it was.

- **Fixed: a battery card printed one health figure twice, as though two sources agreed.** Both cards show "Maximum Capacity" with the raw full-charge-vs-design ratio beneath it, which is worth the space because they come from different places — the headline is the smoothed figure the OS publishes, the caption is arithmetic on the two capacities printed next to it. Both cards also fall back to the raw ratio when the OS figure is missing, and neither noticed when it had. The Mac's caption was printed unconditionally, so on every launch — for as long as `system_profiler` takes to answer, and permanently on a machine where that read fails — the same division appeared twice, rounded to a whole percent above and a tenth below. The iPhone's asked whether iOS had *published* its nominal capacity while the headline asked whether that value was *usable*, and iOS reports it as zero on some devices rather than omitting it.

- **Fixed: one silent Bluetooth accessory could hold every row on "…" for as long as the app ran.** Connecting to an accessory has no timeout and retries indefinitely with no way to ask how it is going, so a device that is listed as connected and never completes the connection stayed outstanding forever. The flag that says "this source has finished answering" is what tells *every* row to stop showing the pending ellipsis and admit it has no battery to report — so a single unreachable mouse hung the whole list, including devices that had answered instantly. The wait is now bounded. The connection attempt is deliberately left running, so a late answer still appears.

- **Fixed: opening the phone panel could make a phone vanish from it.** The window a device may be missing for before its row disappears is derived from how often the app is currently looking, which tightens from 6.5 seconds to 2.5 the instant the panel opens — and it was then applied to a sighting taken up to 5 seconds earlier, under the slower rate, which is already older than the new window before that window exists. One missed enumeration on that tick dropped the row outright and took its cached health with it, on the one tick you are certainly watching. Each sighting now carries the window that was in force when it was taken.

- **Fixed: the Android card's own safety net could never fire, and it greeted you with a stale reading.** It held a flat 5-second window against a keep-warm rate of 10 seconds, so the previous sighting was always twice the age of the window and the first blip in `adb` enumeration dropped the device rather than riding it out — the same defect the iPhone reader was fixed for, in the file that did not get fixed with it, justified by a comment comparing it against an iPhone constant that had already been deleted. It also did not read when the panel opened, on the strength of a comment claiming it matched the iPhone reader, which had stopped being true. Both readers now take their rates from one place. Verified live: 10.0-second gaps with nobody looking, 1.0-second gaps with the panel open and no drift between them, and the forced read landing on the opening edge instead of at the next tick.

Less visible, and the reason this release can be trusted more than the numbers alone suggest: the release scripts now refuse to publish what they used to wave through. The DMG build checks that the two version keys agree before anything is tagged rather than at the last step, when failing means deleting a published release; it refuses an ad-hoc signature, which would silently reset Location and Bluetooth permissions for everyone; and its check of *which* identity signed the build was itself broken about one run in ten, in the direction that hides the problem. The appcast tool now refuses a version that is not newer than the newest item already in the feed. The tested layer is at 166 tests across 12 suites, and every guard added here was checked by breaking it deliberately and confirming the suite went red — 21 of them, which is how the one gap in this release's own tests was found.

_Not notarized — right-click → Open on first launch (on macOS 15+ the confirmation moved to System Settings ▸ Privacy & Security ▸ "Open Anyway"), or `xattr -dr com.apple.quarantine /Applications/StatsBar.app`._]]></description>
      <enclosure url="https://github.com/Matrix-I/stats-bar/releases/download/v2.12.3/StatsBar-v2.12.3.dmg" sparkle:edSignature="2+SySpH4UTnZS+eCWLkSzYBsTqe6DZc2xjsQCyyHhtQ780yyffc6/aLgykOwColeymxzxS3YeF3VHYz8H+pHCg==" length="2542893" type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.12.2</title>
      <pubDate>Sun, 09 Aug 2026 11:51:00 +0700</pubDate>
      <sparkle:version>2.12.2</sparkle:version>
      <sparkle:shortVersionString>2.12.2</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
      <sparkle:fullReleaseNotesLink>https://github.com/Matrix-I/stats-bar/releases/tag/v2.12.2</sparkle:fullReleaseNotesLink>
      <description><![CDATA[# StatsBar v2.12.2 — phone and accessory readings that are there when you look

Another fixes-only release, so another patch number: four fixes and no new panels. Three of them are things you can see, and two of those have been wrong since long before the last release — a headset that never showed its battery at all, and an iPhone card that assembled itself in front of you. `2.13.0` stays reserved for the release that actually adds something.

- **Fixed: the iPhone card's Temperature, Voltage and "Charging with" rows arrived about a second after the panel opened, and grew the card as they did.** With the iPhone menu-bar glyph turned on, four ticks in five ran a cheaper read that queries the lockdown battery domain — and that domain returns seven keys, none of which is Temperature, Voltage or Amperage. Those rows are drawn only when a value exists, so they were *absent* rather than blank, and opening the popover did not start a read of its own: it set a flag and waited for the next tick. So the panel almost always opened on the short shape and reflowed when the full read landed, worst case around 1.9 s later. The cheap read is gone. What paid for it was a once-a-second cadence that iOS cannot honour anyway — it refreshes the underlying battery snapshot every 7 to 20 seconds, measured, so most of those reads were re-fetching an identical one. Reading fully every two seconds instead costs *fewer* subprocesses than the split did, and leaves one row shape for every audience: the card can no longer differ between open and shut, because there is only one thing it can be. The popover keeps its faster rate and now starts a read the moment it opens, which is what the Mac's own battery card has always done. This shipped in v2.10.0, and both the commit that introduced it and the comment on the field that carried it stated the opposite as fact.

- **Fixed: a phone that stops answering could freeze its card, and the Refresh button with it, for up to 41 seconds.** Every call to the underlying tools had a four-second timeout, but nothing bounded the loops around them: three attempts across two registry classes is 24.6 seconds, or 36.6 once a child that ignores the polite signal has to be killed, and enumeration on its own could reach 41.6. The identity read — name, model, iOS version — was a further four calls with no cap, which is the larger half of it. That matters beyond the waiting: the card is written by exactly one path, and that path declines to start while a read is in flight, so for the whole of that window the numbers on screen are frozen and pressing Refresh does nothing, because Refresh goes through the same path. Each probe now gets one wall-clock budget and caps every call at whatever is left of it. Measured against a tool that blocks rather than answers, a wedged device costs 14.3 seconds a cycle instead of 25.0, and a probe whose calls fail quickly — the common transient case the retries were added for — still gets every one of its attempts. The trigger is the ordinary one, named in the code's own comment: a tool blocked in a usbmux call on a locked device.

- **Fixed: Bluetooth Classic headsets never showed a battery level at all.** A Sony WH-CH520 read "—" while macOS knew perfectly well it was at 70% and Control Center said so. The app merged two sources, and a classic headset is invisible to both: it advertises HFP, AVRCP and A2DP with no Bluetooth LE, so CoreBluetooth never enumerates it, and the device records `system_profiler` publishes simply do not carry a battery field for it. macOS puts the classic figure on a third channel — IOKit's accessory power sources — which is now read as well, and which turns out to cover every accessory the other two do, at about a tenth of a millisecond. The old sources are kept rather than replaced, because that coverage has been measured on a mouse, a keyboard and a headset and not on earbuds, and this is not the release to find out the hard way. Two smaller things were wrong in the same place: a device reporting separate left, right and case levels could have all three dropped by the code that was meant to be *adding* a reading, and two accessories sharing a name could each display the other's percentage as though it had been measured.

- **Fixed: the dash in the Bluetooth list meant three different things.** "This device reports no battery", "the reading has not arrived yet" and "the read failed" were all drawn identically, so the dash that correctly described the headset above was indistinguishable from the one a mouse shows in the moment before it displays 90% — and a mouse that goes to sleep and comes back lands in exactly that state. A row now shows "…" while a source is still answering and keeps the dash for a device that genuinely reports nothing, with a tooltip that says so. The distinction is about the device rather than about the app, and it is the same word the overview panel already used one level up.

Less visible, but the reason the first two could be pinned down rather than eyeballed: a device that briefly drops off the bus is no longer dropped from the list. The window meant to ride out that blip was a flat three seconds while two of the four polling rates tick every five and ten, so it could never fire — by the time the next tick looked, the previous sighting was already older than the window. It is derived from the rate in force now, and it has a test that reproduces the old sequence and fails on it. Both fixes were checked by compiling the old and new readers into throwaway programs and running them against the same phone and the same accessories: the previous iPhone reader published 12 of 26 rows with temperature, voltage, amperage and capacity missing, including the row immediately after the popover opened, against 16 of 16 complete afterwards, and the identity fix was confirmed to leave a healthy phone's readings identical field for field. The unit-tested layer now covers the accessory matching and the polling rates as well, at 123 tests across nine suites.

_Not notarized — right-click → Open on first launch (on macOS 15+ the confirmation moved to System Settings ▸ Privacy & Security ▸ "Open Anyway"), or `xattr -dr com.apple.quarantine /Applications/StatsBar.app`._]]></description>
      <enclosure url="https://github.com/Matrix-I/stats-bar/releases/download/v2.12.2/StatsBar-v2.12.2.dmg" sparkle:edSignature="tplXUyL/CWpfvfqfwc/EReVmLKeRCSGLP5F3haHX7H/OsOg95xnkjmkjWy438JPpleGMke0VMEiY+7ApfViIDA==" length="2534642" type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.12.1</title>
      <pubDate>Thu, 30 Jul 2026 11:48:57 +0700</pubDate>
      <sparkle:version>2.12.1</sparkle:version>
      <sparkle:shortVersionString>2.12.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
      <sparkle:fullReleaseNotesLink>https://github.com/Matrix-I/stats-bar/releases/tag/v2.12.1</sparkle:fullReleaseNotesLink>
      <description><![CDATA[# StatsBar v2.12.1 — pinned fan readings and honest units

A fixes-only release: no new panels and no new readings, just three fixes. One of them you will notice every time the battery panel is open; the other two are numbers that were printing units their own tables forbid. A patch number rather than a minor one, because that is what the contents are — the `-SNAPSHOT` in the repo names the next *planned* release, not a commitment, and 2.13.0 stays reserved for the next one that actually adds something.

- **Fixed: the live fan readings scrolled out of sight.** The 🌀 Fans rows were simply the first thing inside the battery panel's scrolling content, so once the panel was tall enough to scroll, reading the Battery, Power, iPhone or Android sections below pushed them off the top of the window. The fans are a property of the Mac rather than of its battery, and they are one of the two bands that move every second — the ⚡ Power rails tick at the same 1 Hz and still scroll with everything else — so they are the band worth keeping in view while reading the rest. They now sit above the scroll area, the way the menu-bar settings and Refresh/Quit footer is pinned below it, and they close with a divider the way the footer opens with one. Two consequences worth naming: the band takes its height out of the scrolling area, so there is a little less room to scroll in exchange for the readings staying in view; and the gap between the fan rows and the 🔋 Battery header is now a divider plus the scroll content's own padding, so it reads a little wider than the old 12pt. Fanless Macs — a MacBook Air — are untouched: no rows, no divider, no reserved space, and the battery header sits flush with the top edge exactly as before.

- **Fixed: sizes and rates could print a figure their own unit rules out.** Three formatters picked the unit from the raw value and then rounded inside that unit, so the top of every band rounded straight past the band. A process between 1023.5 MiB and a gibibyte — including one byte short of it — read "1024 MB" in the RAM panel's per-process list, from a branch that is defined to stop below a gibibyte. 999,950 bytes read "1000.0 KB" in the network session totals, the same figure read "1000.0 KB/s" as a rate in the overview panel's Network row, and 999.6 B/s read "1000 B/s" in the big Download/Upload header — in tables whose next unit begins at 1000. Those now read "1.00 GB", "1.0 MB", "1.0 MB/s" and "1 KB/s". Each window is only half of the last digit actually printed wide, which is why this went unnoticed for so long — but the Download/Upload header draws the value and the unit as separate elements, and it was the one place where a plain "1000" appeared next to a unit that cannot hold it. In the same pass the battery time-remaining formatter picked up the clamp its sibling has always had, so a negative estimate from IOKit can no longer print a figure carrying the sign twice; both call sites already filter those out, so that one is defence for the next caller rather than a reading you have seen.

- **Fixed: on a chip that lays its thermal sensors out differently, the CPU temperature could average a fraction of the die.** Apple Silicon publishes three redundant readings per thermal zone, about 9 °C apart, and StatsBar keeps one of the three. The filter doing that thinning was trusted even when it matched only part of the family, so a chip that instead publishes one sensor per zone — Tp00…Tp07 for eight zones — had six of its eight sensors silently dropped: the CPU temperature became an average over two of them, and only two of the eight cells in the CORES grid carried a temperature at all. It printed a perfectly plausible °C, so nothing downstream could notice. The layout is checked now, and the check is made **per zone** rather than over the whole family — which matters more than it sounds, because the first attempt at this fix judged the family as one unit, and that meant a single anomaly anywhere in it disabled the thinning for every zone. One stray sensor name, or one key missing from a sensor enumeration that skips anything it fails to read, was enough to turn a correct ten-zone reading into an average over all thirty — the same 9 °C error, arriving by the route the fix was written to close, and latched for the whole session because the key list is built once at launch. An anomaly now costs its own zone and nothing else. Being straight about the scope: on the Mac this was measured on, the output is unchanged — the same ten zone keys out of thirty, in the same order — so this is a guard against hardware that has not been observed in the wild rather than a fix for a reading you have seen. Alongside it, a Tp family whose names cannot be decoded now sends the reader to the classic Intel TC** keys instead of handing the per-second read loop keys that fail on every tick.

- **Fixed: release notes were published where nobody could read them.** Every release from 2.6.0 onwards embedded its notes in the update feed in a field this app deliberately does not render, and offered no link to them — so the Changelog button in the update window was hidden, and the notes you are reading are the first in seven releases that the in-app updater can actually open. If you took an update in that range and wondered what changed, that is why.

Most of this release is invisible. The arithmetic behind the memory buckets, the CPU load split, network throughput, hot-battery alert hysteresis, the attached-device bookkeeping, the size and rate formatters and the SMC temperature-key selection now lives in a pure layer with 104 unit tests across seven suites over it; every push to main and every pull request runs that suite and separately compiles, links, signs and verifies the whole app; building a DMG refuses to package a suite that is red or empty; and the steps that publish a release now check themselves — the version, the signature, the signing key, the download URL and the feed's own syntax — instead of relying on whoever is running them. None of that changes anything on screen; the previous implementations were replayed against the new ones, on live readings and on hundreds of thousands of generated ticks, to confirm exactly that. It is the reason the fixes above could be pinned down instead of eyeballed.

_Not notarized — right-click → Open on first launch (on macOS 15+ the confirmation moved to System Settings ▸ Privacy & Security ▸ "Open Anyway"), or `xattr -dr com.apple.quarantine /Applications/StatsBar.app`._]]></description>
      <enclosure url="https://github.com/Matrix-I/stats-bar/releases/download/v2.12.1/StatsBar-v2.12.1.dmg" sparkle:edSignature="KTUKePPeTp2A8hVnjOti5oyT5BB04hp7QZ6ML5vRfWOvGm7KSm+2ZmHXBpdtqyADh/qXL/GswlwRlOItNwxcBA==" length="2512187" type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.12.0</title>
      <pubDate>Wed, 29 Jul 2026 14:26:05 +0700</pubDate>
      <sparkle:version>2.12.0</sparkle:version>
      <sparkle:shortVersionString>2.12.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
      <description><![CDATA[# StatsBar v2.12.0 — per-core detail, fan context, and Free memory told the truth

- **Every logical core now has its own usage bar, with its die temperature above it.** Two cluster averages could never tell one pegged core apart from every core running at a third. One caveat worth knowing: the die publishes more thermal zones than the chip has live cores — ten against eight on a binned M1 Pro — and nothing in the SMC says which zone belongs to which core, so a cell may be showing a neighbour's reading. The spread between cores is only about 3 °C, and the caption names the zone count and the hottest zone so the number that actually matters isn't hidden.

- **Fans now say how hard they are working, not just how fast they are spinning.** 2334 rpm means nothing on its own. Each fan shows its own floor and ceiling and where it currently sits between them, coloured by effort, and a spin-up appears before it becomes audible.

- **Battery cycle count is shown against the pack's rated life** — "307 / 1000 (31%)" instead of a bare 307 that means nothing without the rating.

- **The CPU panel gained thermal pressure, Low Power Mode and load average.** macOS pushes the first two rather than being polled for them, so they cost nothing while the machine is cool and idle. Low Power Mode additionally backs the background sampler off from 2 s to 5 s — a stats app should take that hint rather than work against it. The load average is divided by the core count and labelled as such, so 1.00 means "exactly as much runnable work as this machine has cores".

- **Attached displays are listed:** name, the resolution System Settings shows, the panel's real pixel grid beneath it, refresh rate and diagonal.

- **Values in the panels can be selected and copied.** Serial numbers, MAC and IP addresses, DNS resolvers — the things you open a hardware inspector in order to paste somewhere else — were all dead pixels. Right-click any value for Copy, and right-click a process in the CPU or RAM list to quit it.

- **Fixed: Free memory over-reported by gigabytes.** Cached files — file data the kernel holds on to but can drop the moment something needs the space — were being counted as free. On a 16 GB machine that was 3.2 GB in the wrong column. The bar and legend now carry a Cached Files bucket; the menu-bar percentage is unchanged, because those pages were never counted as used.

- **Fixed: the hot-battery notification did nothing when you clicked it.** It now brings StatsBar forward, where the device that triggered it can be looked at. Two related fixes: the fallback alert played a sound with no setting anywhere to stop it (there is a toggle now, beside the alert switch it belongs to), and on a multi-display desk it could appear over whichever screen happened to have focus instead of beside the menu bar.

- **Fixed: the build did not pin a deployment target,** which silently switched off the compiler's availability checking — a macOS 14-only API would compile without so much as a warning and then crash at runtime on macOS 13. The floor is now declared explicitly and matches what the app says it supports.

_Not notarized — right-click → Open on first launch (or `xattr -dr com.apple.quarantine /Applications/StatsBar.app`)._]]></description>
      <enclosure url="https://github.com/Matrix-I/stats-bar/releases/download/v2.12.0/StatsBar-v2.12.0.dmg" sparkle:edSignature="HJqXhIAEIKgLwRRpf4KSYkDx3n9GlsjeI0+W8nlMKLWoOh/RDrZkAJ0mkuI8PyEZOYR3KaqHBMAjUSxK4D6aCQ==" length="2490915" type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.11.1</title>
      <pubDate>Mon, 27 Jul 2026 11:01:35 +0700</pubDate>
      <sparkle:version>2.11.1</sparkle:version>
      <sparkle:shortVersionString>2.11.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
      <description><![CDATA[# StatsBar v2.11.1 — crash fix

A patch release for one serious bug: **toggling any menu-bar item in the Control Center crashed the app.**

- **Fixed: the app crashed when you switched a menu-bar item on or off.** Flipping Battery, CPU, RAM, Network or Bluetooth in the Control Center could kill the app outright. Writing the toggle's setting posted a preferences-change notification, whose handler assigned `NSStatusItem.isVisible`; AppKit persists the item's position when you do that, which posted the same notification again — an unbounded recursion that exhausted the main thread's stack. The refresh path now guards against re-entry and only writes `isVisible` on a real change. Present since v2.10.0.
- **Fixed: a below-zero battery temperature read as a wildly large positive number.** IOKit reports negative values in the Temperature key as unsigned two's complement, so −5 °C came through as 42,949,667.96 °C — which also made the temperature alert fire about an overheating battery on a freezing one. Positive readings were never affected.

The unconditional `isVisible` write also meant every visible item rewrote a preference once a second; that write is gone, so an idle menu bar now touches user defaults not at all.

_Not notarized — right-click → Open on first launch (or `xattr -dr com.apple.quarantine /Applications/StatsBar.app`)._]]></description>
      <enclosure url="https://github.com/Matrix-I/stats-bar/releases/download/v2.11.1/StatsBar-v2.11.1.dmg" sparkle:edSignature="twobH6vj45AcGtMYHCzCeBw+vTPjDHEIeR/1cabkLi6PBLWFv0QG4ZQ6pl8zm89Ei0TuYSy43SmfKIW/VjJ9Dg==" length="2421163" type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.11.0</title>
      <pubDate>Sun, 26 Jul 2026 15:06:06 +0700</pubDate>
      <sparkle:version>2.11.0</sparkle:version>
      <sparkle:shortVersionString>2.11.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
      <description><![CDATA[# StatsBar v2.11.0 — VoiceOver labels and concurrency hardening

- **The menu-bar items are readable by VoiceOver now.** Each one announces what its glyph is showing — "CPU 34%", "Battery 82%, charging", "Network up 1.2 MB/s, down 340 KB/s" — instead of being an unlabelled button. The label is refreshed only when the value actually changes, so an idle menu bar costs nothing extra.
- **Thread-safety hardening across every reader.** The readers are now compiler-enforced main-thread types, and the iPhone/Android USB enumeration runs in dedicated worker actors that own their own caches. Nothing changes on screen; it removes a class of latent data races around the device caches, which were previously kept safe only by convention.
- **Correct memory figures under translation.** The RAM buckets are now sized with the kernel page size rather than the process page size. Identical on native Apple Silicon and Intel Macs — it only differed for a translated process, where every bucket read four times too small.
- The bundled `cocobat.py` CLI bounds its subprocess calls with a timeout instead of hanging indefinitely on a wedged `ioreg` or libimobiledevice call.

_Not notarized — right-click → Open on first launch (or `xattr -dr com.apple.quarantine /Applications/StatsBar.app`)._]]></description>
      <enclosure url="https://github.com/Matrix-I/stats-bar/releases/download/v2.11.0/StatsBar-v2.11.0.dmg" sparkle:edSignature="DT4uc8ZUoLg8j4UdMntc89Nb53B1YHUbXs+FaG6vmb/3n9+tEHQhZ67Z8ka/CwmIB1oIqQAtCjwik8krHZznBQ==" length="2407456" type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.10.0</title>
      <pubDate>Thu, 23 Jul 2026 23:41:43 +0700</pubDate>
      <sparkle:version>2.10.0</sparkle:version>
      <sparkle:shortVersionString>2.10.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
      <description><![CDATA[# StatsBar v2.10.0 — Reliability fixes across CPU, Network, Bluetooth, and Android

- **CPU usage no longer freezes on long uptimes:** fixed a Mach host-port leak in the CPU reader that, after a day or two of continuous running, could make the CPU readout stop updating until the app was restarted.
- **Bluetooth accessory battery recovers after an off→on toggle:** the battery level of BLE accessories (e.g. a Logitech mouse) no longer sticks at its last value after you turn Bluetooth off and back on — it reconnects and resumes live updates.
- **Menu-bar network rate clears on disconnect:** the throughput shown in the menu bar now drops to 0 when the connection goes away, instead of freezing at the last value while the popover already says "No active network connection".
- **Intel Macs now show CPU temperature:** the classic Intel die-temperature sensors are decoded correctly (they were silently rejected before, leaving the reading blank).
- **Android battery: faster reads and refresh on reconnect:** the learned battery capacity is re-read when you replug a phone, and the capacity read now filters on-device instead of transferring the multi-MB battery-history dump.
- **Bluetooth device list refreshes at the intended rate:** it was updating roughly half as often as intended while the popover was open.
- **Housekeeping:** de-duplicated popover components (CPU/Memory legends, the show-more toggles) and removed dead code.

_Not notarized — right-click → Open on first launch (or `xattr -dr com.apple.quarantine /Applications/StatsBar.app`)._]]></description>
      <enclosure url="https://github.com/Matrix-I/stats-bar/releases/download/v2.10.0/StatsBar-v2.10.0.dmg" sparkle:edSignature="zLVRpQ/xQgaK5xOvTqmoPdrlfz0qbOp537RVvpfOF9aRiQVqYluxnt8antMJYochwGIOTwpnlYsA8sP3pJcwDw==" length="2385135" type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.9.1</title>
      <pubDate>Thu, 23 Jul 2026 22:40:18 +0700</pubDate>
      <sparkle:version>2.9.1</sparkle:version>
      <sparkle:shortVersionString>2.9.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
      <description><![CDATA[# StatsBar v2.9.1

- **More frequent update checks:** the background update check now runs every 6 hours instead of once a day, so new versions arrive sooner. You can still trigger a check any time from the "Check for updates…" button.

_Not notarized — right-click → Open on first launch (or `xattr -dr com.apple.quarantine /Applications/StatsBar.app`)._]]></description>
      <enclosure url="https://github.com/Matrix-I/stats-bar/releases/download/v2.9.1/StatsBar-v2.9.1.dmg" sparkle:edSignature="PagPN4soRFg2VEcifWv08upJXHQeX6WhXL473NWIhtPPnQgC162QWIg+91ALWi2Xa97v1y7uwxNE+j2xJ8O1Cw==" length="2396307" type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.9.0</title>
      <pubDate>Thu, 23 Jul 2026 09:55:03 +0700</pubDate>
      <sparkle:version>2.9.0</sparkle:version>
      <sparkle:shortVersionString>2.9.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
      <description><![CDATA[# StatsBar v2.9.0

Even less background work, plus a batch of robustness fixes.

- **iPhone/iPad:** the menu-bar battery glyph now uses a lightweight read, while the heavier diagnostics read (temperature and detailed battery) runs only while the popover is open — so fewer helper processes fire when a phone is connected but you're not looking at it. Hot-battery alerts stay as responsive as before.
- **Battery:** macOS's "Maximum Capacity" health figure is fetched only while the battery popover is open (it's a detail-only number), so the periodic `system_profiler` call no longer runs in the background.
- **Updates:** information-only Sparkle updates now open their info page instead of offering a broken download, and a mid-download content-length change no longer skews the progress bar.
- **Under the hood:** background-read timing is now on a monotonic clock (immune to system clock changes), the network-interface MAC read avoids undefined pointer access, the DNS server list renders stably when addresses repeat, and the public-IP lookup tidies up its per-fetch network session.

_Not notarized — right-click → Open on first launch (or `xattr -dr com.apple.quarantine /Applications/StatsBar.app`)._]]></description>
      <enclosure url="https://github.com/Matrix-I/stats-bar/releases/download/v2.9.0/StatsBar-v2.9.0.dmg" sparkle:edSignature="a/y/E1AIRqCIwbuQN/CONU2dz+iwO1qI6G3+vzriveHMPJ6TQ11TfWQvMIUVene7txIZORSowRNXSLS6r6jpDg==" length="2396034" type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.8.0</title>
      <pubDate>Wed, 22 Jul 2026 15:38:16 +0700</pubDate>
      <sparkle:version>2.8.0</sparkle:version>
      <sparkle:shortVersionString>2.8.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
      <description><![CDATA[# StatsBar v2.8.0

Less work when you're not looking.

- **Menu-bar toggles now cut real work:** turning a metric off (Battery / CPU / RAM / Network) actually stops its background reader instead of just hiding the icon — so hiding what you don't use lowers CPU and energy. It resumes the instant you turn it back on or open the popover.
- **Lower idle usage:** each menu-bar icon is redrawn only when its value changes (and never while hidden); the battery reads back off to a lazy poll — refreshed immediately on plug/unplug via a power-source notification — and the network reader stops entirely when its rate isn't on screen.
- **iPhone/iPad:** far fewer background helper processes when a phone is connected but nothing is showing it, and the device's name/model are cached instead of re-read every second.
- **Under the hood:** the battery and CPU sensor readers now share a single SMC connection.

_Not notarized — right-click → Open on first launch (or `xattr -dr com.apple.quarantine /Applications/StatsBar.app`)._]]></description>
      <enclosure url="https://github.com/Matrix-I/stats-bar/releases/download/v2.8.0/StatsBar-v2.8.0.dmg" sparkle:edSignature="/B+tn2DAZBn4qt97dY6ndUNkHAJZ/UstXXnBnuP02/SihjpLlmGon4psnAVIrflya41dRNXzIaNsKMZVd5tRAg==" length="2391304" type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.7.0</title>
      <pubDate>Wed, 22 Jul 2026 11:20:39 +0700</pubDate>
      <sparkle:version>2.7.0</sparkle:version>
      <sparkle:shortVersionString>2.7.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
      <description><![CDATA[# StatsBar v2.7.0

More performance and reach.

- **Network:** latency and reachability now work on IPv6-only connections. Previously the ping was IPv4-only, so an IPv6-only link showed the internet as unreachable with no latency even when it was fine.
- **Lower idle usage:** iPhone/iPad and Android battery polling now backs off to a slow keep-warm when neither the popover nor the phone menu-bar item is showing — no more shelling out to libimobiledevice/adb every second for a phone nothing is watching. (Hot-battery alerts for a connected iPhone stay as responsive as before.)
- **Under the hood:** the readers' background-read throttling is unified into one helper, and the SMC sensor reads skip a redundant syscall per key.

_Not notarized — right-click → Open on first launch (or `xattr -dr com.apple.quarantine /Applications/StatsBar.app`)._]]></description>
      <enclosure url="https://github.com/Matrix-I/stats-bar/releases/download/v2.7.0/StatsBar-v2.7.0.dmg" sparkle:edSignature="HSCnj+hTFlzmiTt5m8irkOGdKO2diRsKm1MoMBdiAPa8bxyjnHHJRuMQ6NgbXOZQszZVn9hPad+Po1EnmXXJCg==" length="2380855" type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.6.0</title>
      <pubDate>Wed, 22 Jul 2026 10:52:14 +0700</pubDate>
      <sparkle:version>2.6.0</sparkle:version>
      <sparkle:shortVersionString>2.6.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
      <description><![CDATA[# StatsBar v2.6.0

Performance and polish.

- **Android (multi-device):** fixed battery health being dropped or misreported when more than one Android phone is connected — each device's state is now tracked independently.
- **CPU:** the frequency readout no longer shows a wrong value for one tick right after reopening the popover.
- **Lower idle usage:** battery power-rail/fan SMC reads and iPhone/iPad USB polling now back off while their panel is closed, cutting background work.
- **UI:** the Android "show more" expand no longer stutters; removed a duplicate "Status" row in the Network panel.

_Not notarized — right-click → Open on first launch (or `xattr -dr com.apple.quarantine /Applications/StatsBar.app`)._]]></description>
      <enclosure url="https://github.com/Matrix-I/stats-bar/releases/download/v2.6.0/StatsBar-v2.6.0.dmg" sparkle:edSignature="bjhFOBbdmH9Mj+Bk764i2RXzCOrYhaBmTSRP6KqnZkWkvjkHYHPCQEkbwzOmF8L8nWPEibx50x/Js6Mw8PNvDg==" length="2377265" type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.5.0</title>
      <pubDate>Tue, 21 Jul 2026 23:52:15 +0700</pubDate>
      <sparkle:version>2.5.0</sparkle:version>
      <sparkle:shortVersionString>2.5.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
      <sparkle:releaseNotesLink>https://github.com/Matrix-I/stats-bar/releases/tag/v2.5.0</sparkle:releaseNotesLink>
      <enclosure url="https://github.com/Matrix-I/stats-bar/releases/download/v2.5.0/StatsBar-v2.5.0.dmg" sparkle:edSignature="3/7c5tQq96VgBIWLJDchiN2CW4Ae6Idx8fwykJ+tXIZzjMjL592qEQgxpwkC5tDFkYOR5auDx4AFyW46oP5eDg==" length="2375825" type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.4.0</title>
      <pubDate>Tue, 21 Jul 2026 22:43:12 +0700</pubDate>
      <sparkle:version>2.4.0</sparkle:version>
      <sparkle:shortVersionString>2.4.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
      <sparkle:releaseNotesLink>https://github.com/Matrix-I/stats-bar/releases/tag/v2.4.0</sparkle:releaseNotesLink>
      <enclosure url="https://github.com/Matrix-I/stats-bar/releases/download/v2.4.0/StatsBar-v2.4.0.dmg" sparkle:edSignature="lxdnWIt3M6qpeEQaVvi0QKvRDXRXdCbdyymB0OmGIKB+uVAoXnNDCf2gt9+R7XVISvoUUVaH9TCU7sqq5eBuDA==" length="2316701" type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.3.0</title>
      <pubDate>Tue, 21 Jul 2026 22:11:15 +0700</pubDate>
      <sparkle:version>2.3.0</sparkle:version>
      <sparkle:shortVersionString>2.3.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
      <sparkle:releaseNotesLink>https://github.com/Matrix-I/stats-bar/releases/tag/v2.3.0</sparkle:releaseNotesLink>
      <enclosure url="https://github.com/Matrix-I/stats-bar/releases/download/v2.3.0/StatsBar-v2.3.0.dmg" sparkle:edSignature="G2GevQYE4cmP4IoehTEfNRJA1LcMgaNo8+jmNqT76n+wttaBSRn2IooY93bYrz5vyTuWwuyk4iI8yhFiNZxkAQ==" length="2314881" type="application/octet-stream" />
    </item>
  </channel>
</rss>
