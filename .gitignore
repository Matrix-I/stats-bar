StatsBar.app/
StatsBar
*.dmg
!releases/*.dmg
# Sparkle is fetched by fetch_sparkle.sh (pinned version), not committed — keeps the repo lean.
Frameworks/
.sparkle-tools/
__pycache__/
*.pyc
# SwiftPM's build dir. Package.swift exists only to run `swift test` (see its header) — the app is
# still built by build_app.sh, which never writes here.
.build/
.claude/
.idea/
AppIcon.iconset/
AppIcon_1024.png
icon_gen
.DS_Store
